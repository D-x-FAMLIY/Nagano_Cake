<div class="container">
  <div class="row">
    <div class="col-md-3">
      <%= form_with model: @genre, local: true %>
      <h4>カテゴリー名</h4>
      <%= f.label :name %>
      <br>
      <%= f.text_field :neme, class: 'form-control' %>
      <br>
      <%= f.submit '追加',class: 'btn btn-primary btn-block' %>
      <% end %>
    </div>
  </div>
</div>