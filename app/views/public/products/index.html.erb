<div class="container">
  <div class="row">
    <div class="col-md-3">
      <table>
        <tr>
          <th>ジャンル検索</th>
        </tr>
        <% @genres.each do |genre| %>
          <tr>
            <td><%= genre.name %></td>
          </tr>
        <% end %>
      </table>
    </div>
    <div class="col-md-8">
      <table>
        <tr>
          <th>
            <h3>商品一覧</h3>
            <p>(全<% @products.count %>件)</p>
          </th>
        </tr>
        <% @products.each do |product| %>
          <tr>
            <td></td>
            <td><%= product.name %></td>
            <td><%= product.price_without_tax %></td>
          </tr>
         <% end %>
      </table>
    </div>
  </div>
</div>

