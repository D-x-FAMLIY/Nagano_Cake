<div class="container">
  <div class="row">
    <div class="col-md-3">
      <table>
        <tr>
          <th>ジャンル検索</th>
        </tr>
        <% @genres.each do |genre| %>
          <tr>
            <td><%= link_to genre.name, public_products_path %></td>
          </tr>
        <% end %>
      </table>
    </div>
    <div class="col-md-9">
      <table>
        <tr>
          <th>
            <h3>商品一覧</h3>
            <p>(全<% @products.count %>件)</p>
          </th>
        </tr>
        <% @products.each do |product| %>
          <tr>
            <td></td>
            <td><%= link_to product.name, public_product %></td>
            <td><%= product.price_without_tax %></td>
          </tr>
         <% end %>
      </table>
    </div>
  </div>
</div>

